# BOX edition — canonical gateway env fragment for on-prem deploy (NFR-560).
# Merge into the gateway Deployment/Helm values for the коробка; runtime startup
# rejects BOX boot unless GATEWAY_PROJECT_ACCESS_ENFORCE=true (FR-PROJ-065).
GATEWAY_PROJECT_ACCESS_ENFORCE=true
